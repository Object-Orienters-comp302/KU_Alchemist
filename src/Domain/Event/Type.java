package Domain.Event;

/**
 * Enum defining different types of events.
 * These types are used to categorize events in the system.
 */
public enum Type {
    GOLD,
    ARTIFACT,
    POTION,
    INGREDIENT,
    DECK_ARTIFACT,
    DECK_INGREDIENT,
    SICKNESS,
    START_MENUVIEW,
    REPUTATION,
    FORAGERIGHT,
    PAUSE
    
}
